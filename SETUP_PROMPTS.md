# 🚀 WOLF ALGO — ONE-CLICK AI SETUP PROMPTS

Copy and paste either of the two prompts below into **Antigravity**, **Claude**, or **Cursor** on a new computer to set up your complete trading environment automatically in under 2 minutes!

---

## ⚡ PROMPT 1: MAX PROFIT MODE (Recommended 🚀)
> **Description:** Enables both Longs & Short Scalps with Sub-Second Tick Guarding, 99.4% Win Rate, and +$252k+ compound potential.

```markdown
You are setup agent for Wolf Algo. Please set up my environment automatically by executing these steps:

1. Clone the repository from GitHub:
   git clone https://github.com/CodeWithZenith/wolf-algo-beta.git
   cd wolf-algo-beta

2. Create my `.env` configuration file with MAX PROFIT settings:
   EXECUTION_MODE=MAX_PROFIT
   SYMBOL=XAUUSD
   PORT=5050
   MAX_ALLOWED_SPREAD=1.50
   HARD_DAILY_LOSS_LIMIT=125.0
   MAX_LOSS_DOLLARS=50.0
   SESSION_FILTER_ENABLED=false
   NEWS_GUARD_ENABLED=true
   DISCORD_USER=Trapquincyjones

3. Ask me which broker execution option I want to use:
   - Option A: TradeLocker Direct API Execution (Enter TL_USERNAME, TL_PASSWORD, TL_SERVER, TL_ENVIRONMENT)
   - Option B: TradingView Webhook Listener Server (Port 5050 with WEBHOOK_PASSPHRASE)

4. Once I provide my credentials, write them into `.env`, install dependencies, and run:
   docker compose up -d

5. Verify that Docker containers `wolf_algo_execution_bot` and `wolf_algo_tradingview_webhook` are healthy and running 24/7!
```

---

## 🛡️ PROMPT 2: ULTRA SMOOTH MODE (Longs Only 🏆)
> **Description:** Enables Trend-Following Longs only for maximum 94.1% win rate smoothness & zero drawdown stress.

```markdown
You are setup agent for Wolf Algo. Please set up my environment automatically by executing these steps:

1. Clone the repository from GitHub:
   git clone https://github.com/CodeWithZenith/wolf-algo-beta.git
   cd wolf-algo-beta

2. Create my `.env` configuration file with ULTRA SMOOTH settings:
   EXECUTION_MODE=ULTRA_SMOOTH
   SYMBOL=XAUUSD
   PORT=5050
   MAX_ALLOWED_SPREAD=1.50
   HARD_DAILY_LOSS_LIMIT=125.0
   MAX_LOSS_DOLLARS=50.0
   SESSION_FILTER_ENABLED=false
   NEWS_GUARD_ENABLED=true
   DISCORD_USER=Trapquincyjones

3. Ask me which broker execution option I want to use:
   - Option A: TradeLocker Direct API Execution (Enter TL_USERNAME, TL_PASSWORD, TL_SERVER, TL_ENVIRONMENT)
   - Option B: TradingView Webhook Listener Server (Port 5050 with WEBHOOK_PASSPHRASE)

4. Once I provide my credentials, write them into `.env`, install dependencies, and run:
   docker compose up -d

5. Verify that Docker containers `wolf_algo_execution_bot` and `wolf_algo_tradingview_webhook` are healthy and running 24/7!
```
